# Backlog — after orchestrator-core (Plan 1)

Triaged by final whole-branch review, 2026-08-11. Plan 1 delivered: headless orchestration core, sim fleet on real VDA 5050/MQTT, PIBT routing + Hungarian dispatch + reservations, order lifecycle, 150+ tests green.

## P0 — prerequisite for any real-hardware pilot

1. **~~Extension-flood / horizon gating / reservation enforcement.~~ FIXED** (commits `9f5d78f`, `30355ee`, `a808916`, `290d976`, `085288d`). `Orchestrator.runRouting()` (packages/orchestrator/src/orchestrator.ts:642-692) used to append one node + re-send a stitching order EVERY tick regardless of robot catch-up. Consequences (measured in sim soak): throughput collapse ~20s/cell on legs >5 cells; sustained same-cell overlaps under wall-clock (45 in one run); occasional permanent reservation deadlock (foreign-owned current cell → empty grant forever). Reservation claims were advisory (didn't block sendMission) — contradicted spec's "router optimism never bypasses reservations". Fixed via horizon-gated frontier extension (only extends when the robot is within `opts.horizon` nodes of its commanded frontier), reservation claims now block extension (full-grant-or-nothing), a deadlock backstop force-requeues genuinely stuck robots (`opts.blockedTicksLimit` consecutive blocked ticks), and a frontier-race resume path (using both `rt.currentNodeId` and `rt.lastVdaNodeId` as signals) so a robot that's genuinely on its committed path resumes instead of being wrongly requeued. The regression gate at packages/sim/test/e2e.test.ts (`expect(collisions.sustained).toEqual([])`) is restored and green: 0 sustained/soft collisions across repeated runs. The 10-robot/20-order soak sits at exactly the 95% completion gate (19/20) — the one non-completing order in every observed run is the parked-idle-robot limitation, see new P2 item below, not a recurrence of the extension flood. A new 1-robot/9-cell latency test (packages/sim/test/e2e.test.ts) proves the fix directly: ~9.0-9.1s wall for a leg that took >180s (~20s/cell) pre-fix.
14. **~~No reconciliation/alarm when a legged robot's reported node falls off its committed window.~~ FIXED** (commits `cb425db`, `08bdff2`, `c9f4dce`). Nothing in `Orchestrator` used to detect or alarm when a legged robot's `currentNodeId` resolved to a node that wasn't part of its own `leg.nodeIds` window (real-world avoidance swerves, manual/teleop moves, or a bumped AGV) — the pipeline just kept trusting the last-known committed path as if nothing happened. Fixed via an off-window reconciliation check in `runRouting()`'s progress scan: two consecutive ticks where `currentNodeId`/`lastVdaNodeId` are found nowhere in `leg.nodeIds` (transient-snap-tolerant via `leg.offWindowTicks`) triggers `recoverLeg()` — alarm, cancel, requeue order (or clear a yield leg), `releaseAll` + own-cell re-claim. Covered by unit tests in orchestrator.test.ts ("off-window reconciliation (#14)").
15. **~~No physical-progress watchdog for a robot stalled on a fully-extended path.~~ FIXED** (commits `cb425db`, `08bdff2`, `c9f4dce`). A robot that stopped moving while genuinely `EXECUTING` a mission whose frontier already equalled the leg goal (nothing left to extend) had no backstop: `runRouting()`'s horizon/contention-blocked branches never ran (no pending extension to block), so a stuck-but-still-connected robot just silently sat there holding its order forever, with no timeout until the connection itself eventually dropped. Fixed via `RobotLeg.lastProgressTick` (set at leg creation, updated on every `progressIndex` advance and on frontier-race resume) and a watchdog check in `runRouting()`: `tickCount - leg.lastProgressTick >= opts.watchdogTicks` (default 40, intentionally above `blockedTicksLimit`'s 20 so the contention backstop wins where both apply) triggers the same recovery path as #14. Covered by unit tests in orchestrator.test.ts ("physical-progress watchdog (#15)").

## P1 — before Plan 2 features build on top

2. **~~Build pipeline.~~ FIXED** (commits `d7fce71`, `c3abbd0`, cleanup-wave Task 4). `tsup` (transpile-only, `bundle: false`, dts off) wired per node package (`shared, core, robot-interface, orchestrator, persistence, api, sim`) via a shared root `tsup.config.ts`; `package.json` `exports` maps give each package a `development` condition (source, for tsx/vitest) and a `default` condition (`dist/`, for bare `node`). Root `pnpm build` runs `pnpm -r --filter '!@tez/dashboard' build` in dependency order; dashboard keeps its own untouched `vite build`. Verified: `node packages/sim/dist/cli.js`, a bare-node smoke of `@tez/robot-interface`'s dist barrel + `@tez/sim` fleet against a real dev broker, `node packages/api/dist/main.js` booting demo mode headless, and `pnpm demo`/`pnpm -r test` unaffected (dev flows still resolve source via the `development` condition). Follow-up fix (`c3abbd0`): `scripts/demo.mjs`'s two `tsx` spawns pinned to `--conditions=development` so they resolve sibling packages to source too (tsx doesn't auto-add `development` the way Vite/vitest do) — otherwise a clean checkout without a prior `pnpm build` would `ERR_MODULE_NOT_FOUND` on `pnpm demo`.
3. **~~PIBT boundary-corridor oscillation~~ FIXED** (commit `9f5d78f`) on uncurated layouts (two agents swapping through narrow boundary corridor could oscillate). Fixed by having PIBT prefer unoccupied cells on distance ties, ending the corridor livelock. Verified by the restored soak/latency tests in packages/sim/test/e2e.test.ts (see P0 item 1 above).
4. **~~Adapter contract doc drift~~ FIXED** (commit `6cbb67e`) (packages/robot-interface/src/adapter.ts:29-34): per-tick ordering text only FakeAdapter could honor; orchestrator compensates (premature-done guard). Rewrote the `on()` contract to state the weaker real-adapter guarantee (per-robot event order only; no tick alignment; premature missionDone and stale-holder reports are expected, compensated by the I1 premature-done guard and C1 stale-reporter guard) versus FakeAdapter's stronger lockstep one; flagged FakeAdapter's new-mission position teleport as fake-only in a matching class-level JSDoc in fake.ts. Matters before writing adapter #3 (seer-tcp).
5. **~~MAP_ID "warehouse" duplicated~~ FIXED** (commit `15315de`) in vda5050.ts:106 + sim/src/fleet.ts:16. Moved to `export const DEFAULT_MAP_ID = "warehouse"` in @tez/shared; both call sites import it.

## P2 — hygiene / operational

6. `.npmrc workspace-concurrency=1` — keep (vda-5050-lib stop-race under parallel vitest); CI note: `pnpm -r test` serialized ~8-10 min.
7. **~~Contention alarm prints wrong cell's owner~~ FIXED** (commit `a808916`, denied-cell from deduped window). orchestrator.ts:678 used to print `owner(aheadCell)=undefined`; actual blocker was the foreign-owned current cell. Fixed by comparing the granted-reservation count against the DEDUPED distinct-cell list (a revisited window previously both mis-detected a full grant as partial and named the wrong denied cell) and indexing the denied cell from that same deduped list — see orchestrator.ts's `dedupedCells`/`deniedCell` computation in `runRouting()`.
8. **~~Router `priorities` map never pruned; `Vda5050Adapter.stop()` doesn't clear tracking maps~~ FIXED** (commit `15315de`). `attemptCounters` unbounded growth remains documented/accepted (not in scope). Fixed: `PibtRouter.step()` now deletes priority entries for agent ids not present in that step's agent set at the end of each step (re-appearing agents re-seed; determinism within a run preserved); `Vda5050Adapter.stop()` clears its per-robot tracking maps so a stopped adapter doesn't pin memory.
9. **~~Self-edge `[A,A]` wire orders on PIBT stay-moves~~ VERIFIED NOT A BUG** — closed with evidence, no code change needed. `runRouting()`'s extension only appends `nextNode` when `nextNode !== frontierNode` (orchestrator.ts, frontier-extension block); initial legs seed `nodeIds` with a single node (the robot's current node); the frontier-race resume path (handleMissionDone's `atFrontier` branch) also reseeds to a single node (`[frontierNode]`); yield legs (#13) likewise seed with a single node (`[blocker.currentNodeId]`). No code path can ever push a duplicate of the leg's last element, so no self-edge can reach `sendMission()`. Pinned by a permanent regression assertion added to orchestrator.test.ts's "every sent path is a chain of adjacent nodes" test: for every mission actually sent to the adapter across a skewed-cadence, two-robot, two-order run, `nodeIds[i] !== nodeIds[i-1]` for all i.
10. **~~Offline-within-grace robot still dispatchable (burns a retry); quarantine ERROR status clobbered by next heartbeat (observability).~~ FIXED** (commit `53a2409`, cleanup-wave Task 1). `runDispatch`'s idle gate now also requires `rt.online`, so a robot inside its offline grace window is no longer handed a new order it can't execute. Separately, while `rt.quarantined` is set, incoming state events no longer overwrite `rt.state.status` back from `"ERROR"` — position/battery updates keep flowing, only `status` is pinned until un-quarantine, so the quarantine signal survives the robot's own next heartbeat.
11. **~~`RobotId` defined twice; root README doesn't mention packages/; edge-midpoint snap.~~ FIXED (cheap parts, this commit, cleanup-wave Task 5)** (edge-midpoint snap already noted above as subsumed by the P0 fix). Root README now has a package-by-package map of all 8 `packages/*` workspaces. `RobotId`'s dual definition (plain `string` alias in `@tez/shared` vs. branded type in `@tez/core`) is now documented in place at the cast-bridge site (`asCoreRobotId()`, packages/orchestrator/src/orchestrator.ts) as an accepted, deliberate bridge — **type unification itself is deferred**, assessed as a risky refactor with no behavioral payoff (both aliases are `string` at runtime; the cast is a no-op).
12. Battery/charging: sim uses VirtualAgvAdapter defaults; explicit charge-action orchestration unimplemented. Spec KPIs distance/queue-depth not in snapshot().
13. **~~Parked idle robot on the only path drives orders into backstop-requeue.~~ FIXED** (commits `434722d`, `8c51327`). A parked IDLE robot sitting on the only path into an order's pickup/drop permanently owned that cell but was never itself commanded to move out of the way — PIBT only ever pushed it virtually (treated it as an obstacle for planning), never dispatched an actual parking/yield move. Such an order blocked every extension attempt until the deadlock backstop (`opts.blockedTicksLimit` consecutive fully-blocked ticks, default 20) requeued it; after 3 requeues `OrderBook` failed it outright rather than ever routing around the parked robot. This was the exact cause of the 10-robot/20-order soak's one non-completing order (19/20 = 95.0%, sitting exactly at the regression gate). Fixed via yield-leg dispatch: `RobotLeg` gained `kind: "order" | "yield"`; when a blocked ORDER leg's `blockedTicks` reaches `opts.yieldAfterTicks` (default 6), a qualifying blocker (online, un-quarantined, leg-less robot owning the contested cell) is BFS-routed (depth cap 10) to a free, unoccupied node and dispatched a `kind:"yield"` leg via the same reservation-gated leg machinery as order legs (no collision-invariant change); yield completion is handled explicitly in `handleMissionDone`/`handleMissionFailed` by missionId shape (`yield:` prefix), before order-book logic, and never touches the order book. Anti-thrash via `rt.lastYieldTick`/`opts.yieldCooldownTicks` (default 20). Covered by unit tests in orchestrator.test.ts ("yield dispatch (#13)").
    - **Soak gate raised 0.95 → 1.0 (Task 5, pre-pilot-hardening, 2026-08-11).** Two consecutive full-workspace acceptance runs (`corepack pnpm -r test`) both landed the 10-robot/20-order soak at 20/20 (100.0%), 0 sustained/soft collisions, 0 `requeueing order`/backstop alarms attributable to parked robots (yield alarms present and expected — 3 per run). Run 1: wallMs=19555, alarms=362 (contention=210, committed-path-complete=147, yield=3, plus 2 one-off premature-missionDone entries from tick-boundary timing, not requeue/backstop). Run 2: wallMs=13837, alarms=269 (contention=162, committed-path-complete=104, yield=3). Failure-injection held 9/10 (90.0%) both runs (gate is 70%, unaffected). Latency test: 9034ms both runs. Per criteria (two consecutive 20/20 + deterministic completion count), the completion-rate gate in `packages/sim/test/e2e.test.ts` was raised from `toBeGreaterThanOrEqual(0.95)` to `toBeGreaterThanOrEqual(1.0)`. If a future run drops below 20/20, capture the alarm histogram before touching this gate — do not lower it without a documented regression.

## P2 — pre-pilot findings (final whole-branch review, 2026-08-11, round 3)

16. **Off-window recovery (#14) can leave a drifted robot cell-less inside another robot's already-granted committed window.** `recoverLeg()`'s own-cell re-claim (`executeRecovery()`) claims whatever `rt.currentNodeId` resolves to at recovery time — if the drift genuinely carried the robot onto a cell another robot's leg has ALREADY validly claimed as part of its own committed extension window (not a collision-hole bug, just two robots' true positions and reservation state disagreeing after an off-path event), the re-claim legitimately returns an empty grant (claim() is a no-op when the first cell is foreign-owned) and the drifted robot is left owning NOTHING — the existing "idle self-claim of X DENIED... collision-hole invariant violated" alarm fires, but nothing auto-halts the fleet. Runbook implication until an auto-halt mechanism exists: pilot operators must treat that exact alarm string as a hard STOP-and-inspect signal, not a log line to shrug off — a robot with no cell ownership at all is a genuine physical-safety gap (nothing prevents another robot's committed path from being granted through wherever it's actually standing). Add to the pre-hardware-pilot runbook explicitly; revisit once an automatic fleet-pause-on-invariant-violation exists.
17. **Same-tick duplicate yield-target selection across two independently blocked robots.** `tryDispatchYield()` is called once per blocked robot per tick it crosses `yieldAfterTicks`; if TWO different robots become blocked by the SAME parked robot (or by two different parked robots that both BFS to the same nearest free cell) in the same tick, both calls run against the reservation table as it stood at the START of this tick's `runRouting()` pass — `findYieldTarget()` doesn't see the OTHER call's just-assigned target (leg assignment happens synchronously inside the loop, but the second call's BFS re-derives "unowned" freshly each time, so it CAN legitimately pick the same free cell the first call just picked before that first blocker has had any tick to actually claim/occupy it via reservation). This is safe — not a collision risk — because actual movement is still gated by the ordinary reservation-claimed extension in `runRouting()`: whichever of the two yielded robots reaches the reservation `claim()` for that shared cell first (via normal PIBT/priority resolution) wins it; the other's yield leg simply blocks (counted as an ordinary blocked yield leg, eventually recovered by the deadlock backstop if it can't find another route) exactly like any other contested-cell scenario. Net effect observed: a brief bit of thrash (one extra blocked-then-abandoned yield leg) in a rare double-blocker coincidence, never a safety issue. Left as-is; revisit only if soak telemetry ever shows this thrashing measurably hurting throughput.
18. **Soak alarm-cap (#8, `pushAlarm()`'s 500-entry ring buffer) could someday false-fail the failure-injection test's `sawOfflineRequeue` check if a run's alarm count exceeds the cap.** That check scans `getAlarms()` for a specific substring; if the cap has evicted the alarm that would have matched (because 500+ OTHER alarms accumulated first), the assertion would wrongly read "never happened" when it did. Not observed in practice — the largest alarm count seen across repeated soak+failure-injection runs to date is 362 (see item 13's soak-gate note above), comfortably under the 500 cap — but there's no structural guarantee against a future, noisier scenario (more robots, longer runs, tighter contention) pushing a run's alarm count past 500 before the specific event the test is checking for occurs. If this test ever starts flaking, check the alarm count/histogram before assuming a real regression — it may just be the cap silently evicting the evidence.

## P2 — Plan 2 findings (api + dashboard, Task 14 final review, 2026-08-11)

13. **~~`aedes`'s ESM default-import breaks bare-`node` runs of `robot-interface` consumers.~~ FIXED** (commit `70a1360`, cleanup-wave Task 3). `dev-broker.ts` no longer does a top-level `import { createBroker } from "aedes"`; `startDevBroker` now does a lazy `await import("aedes")` and destructures from the default export, which survives both tsx/vitest and plain `node`. Proven with a bare-node smoke test in Task 4's build verification (`@tez/robot-interface`'s built dist barrel importing `startDevBroker` and standing up a real dev broker under plain `node`, no tsx). Barrel export of `startDevBroker` unchanged.
14. **~~`demo:vda` AGV-id mismatch + missing `BROKER_URL` log line.~~ FIXED, demo-mode scope only — vda-mode id mismatch remains, see new item below** (commits `4c63c59`, `67d0c19`, cleanup-wave Task 2). Per spec decision 4, `packages/api`'s **demo-mode** composition (`buildDemoSystem`, packages/api/src/system.ts) now seeds `FakeAdapter` robots as `sim-001..sim-00N`, matching `spawnFleet`'s scheme — one id scheme end to end for `DEMO` (`pnpm demo`). `packages/api/src/main.ts` now logs a `BROKER_URL=<url>` line in `vda` mode; `scripts/demo.mjs` discovers the broker from it instead of falling back to the 5s-grace hardcoded default (follow-up commit `67d0c19` fixed a listener-registration ordering bug in `demo.mjs` so it doesn't miss that log line). `pnpm demo` verified booting with consistent ids. **Residual gap this fix did not touch:** `buildVdaSystem` (packages/api/src/system.ts:105-108) still seeds its `Vda5050Adapter` AGV serials as `r1..r${config.robots}` (unchanged), while `@tez/sim`'s `spawnFleet` mints `sim-001..sim-00N` — in `vda` mode (`pnpm demo:vda`) the two id schemes still disagree, so the sim fleet's robots are foreign/unconfigured AGVs to the adapter and are ignored (`robotsOnline: 0`, dashboard shows an empty fleet even with the sim process running fine). Tracked as item 20 below.
15. **~~Root `vitest.workspace.ts` runs `packages/*/test` in `environment: "node"`.~~ FIXED** (commit `d6191e0`, pre-dates cleanup-wave, verified in this task). Root `vitest.workspace.ts` now globs `packages/*/vitest.config.ts` first (picking up each package's own config, incl. dashboard's `happy-dom`/`api`+`persistence`'s custom options) and falls back to an inline `environment: "node"` entry — explicitly excluding `api`, `dashboard`, `persistence` — only for the plain node packages without their own config file. Re-verified in this task: both full-workspace `corepack pnpm -r test` runs (cleanup-wave Task 5 acceptance) show `packages/dashboard test: Tests 42 passed (42)` running under the root runner, not skipped or excluded — root and per-package (`pnpm --filter @tez/dashboard test`) invocations agree.
16. **Alarm strings are raw, untranslated English orchestrator log lines.** `StateFrame.alarms` (`packages/api/src/ws.ts`) forwards the orchestrator's alarm strings verbatim; `AlarmDrawer` (`packages/dashboard/src/components/AlarmDrawer.tsx`) renders `alarms: string[]` directly with no lookup through `i18n.ts`'s `STRINGS`/`t()`. Under RU/UZ UI, alarm text still reads in English. Fix needs either a structured alarm shape (code + params) from the orchestrator that the dashboard can map through `t()`, or a client-side regex/keyword translation table as a stopgap.
17. **~~Orchestrator lacks a public `cancelOrder` API.~~ FIXED** (commits `53a2409`, `4c63c59`, cleanup-wave Tasks 1+2). `Orchestrator.cancelOrder(orderId: string): TransportOrder` implemented per `docs/PLAN2-HOOK-REQUESTS.md` §1: any non-terminal order transitions to `canceled` via `book.transition`; if a robot holds it, best-effort `adapter.cancelMission`, `releaseAll` + own-cell re-claim, `setLeg(undefined)`; throws on unknown/terminal order. `DELETE /orders/:id` (`packages/api/src/routes/orders.ts`) now calls it: 200 with the order, 404 unknown, 409 already terminal — no more `501`.

## P2 — Plan 2 findings (final whole-branch review, Task 15/16, 2026-08-11)

18. **~~WS frame payload grows monotonically.~~ FIXED** (commit `53a2409`, cleanup-wave Task 1). `Orchestrator.snapshot()` now returns every non-terminal order plus only the most recent `opts.terminalOrderRetention` (default 200) terminal orders (completed/failed/canceled), newest-last, stable order; the internal `allOrders` list itself is pruned in memory too (not just the returned view) — on transition to terminal, if the terminal count exceeds retention, the oldest terminal orders are dropped from `allOrders`. KPIs (`completions`, cycle times) are tracked separately and unaffected. `makeFrame`/`ws.ts` needed zero changes — the fix is entirely orchestrator-side, matching the "no public-API breaks for dashboard" acceptance criterion (snapshot shape unchanged, orders just bounded). `OrderBook` internals untouched.
19. **No production serving story for the dashboard.** The only way to run `@tez/dashboard` today is `vite dev` (see `packages/dashboard`'s `pnpm dev`) with its dev-server proxy to `@tez/api`; `packages/api` never serves a built `dist/` (no static-file plugin registered, no `vite build` step wired into `api`'s startup). Fine for the filmed demo (`pnpm demo` boots both dev servers), but this needs a real answer — `@fastify/static` serving `packages/dashboard/dist` behind the same origin as the API, or a separate reverse proxy — before any real-hardware pilot deploys the dashboard anywhere but a developer's laptop.

## P2 — final fix wave findings (cleanup-wave review round 4, 2026-08-12)

20. **~~`buildVdaSystem` AGV serials still `r1..r3`; vda-mode sim fleet not recognized (residual from #14).~~ FIXED** (this commit). `buildVdaSystem` (packages/api/src/system.ts) now seeds `agvIds` with `serialNumber: sim-001..sim-00N`, matching `@tez/sim`'s `spawnFleet` scheme, with an in-place comment tying the two (the adapter's foreign-AGV-serial guard drops events from unconfigured serials). Re-smoked `pnpm demo:vda` end to end: `/health` reports `robotsOnline: 3, degraded: false` (was 0), `BROKER_URL=mqtt://localhost:1883` discovered with no fallback warning, and a submitted order (`POST /orders` n2_2→n5_5) ran to `completed` through the real MQTT/VDA5050 path.
21. Unify the api/sim dist producer used by `scripts/demo.mjs`: it currently runs a raw `tsc -p tsconfig.build.json` directly for both `@tez/api` and `@tez/sim` (demo.mjs, around the two `run("corepack", ["pnpm", "--filter", ..., "exec", "tsc", "-p", "tsconfig.build.json"])` calls), a second, separate build pipeline from each package's own `pnpm build` script (`tsup --config ../../tsup.config.ts`, see item 2/#2 above). Point demo.mjs at the package build scripts (`pnpm --filter @tez/api build`, `pnpm --filter @tez/sim build`) instead, so there's exactly one dist-producing pipeline to keep in sync.
22. **~~VDA `BROKER_URL` 5s fallback timer armed at spawn time, racing the api's entire boot.~~ FIXED** (this commit). `scripts/demo.mjs` keeps the line listener attached at spawn time (required — see the comment block above `mqttUrlPromise`) but the 5s fallback countdown is now armed via `armMqttFallback()` only after `waitForHealth` resolves, so it races the log line itself, not the api's boot+listen time. If the line already arrived before arming, the fallback is a no-op (`resolved` guard). Verified in the same `pnpm demo:vda` smoke as #20: broker url discovered, no fallback warning.
23. Replace `cancelOrder`'s message-prefix error mapping with typed errors. `Orchestrator.cancelOrder` throws plain `Error`s with string messages (`"order not found: ..."`, `"order already terminal: ..."`); `packages/api/src/routes/orders.ts` maps them to HTTP 404/409 via `message.startsWith(...)` (lines ~166-171). Fragile to message wording changes. Introduce `OrderNotFoundError`/`OrderTerminalError` classes in `@tez/orchestrator` (or `@tez/core`) and have the route branch on `instanceof` instead.
24. Extract a shared `isTerminalStatus(status)` predicate. The check `status === "completed" || status === "failed" || status === "canceled"` is duplicated three times: `packages/core/src/orders.ts:230`, `packages/orchestrator/src/orchestrator.ts:387`, and `packages/orchestrator/src/orchestrator.ts:433`. Factor into one exported helper (likely in `@tez/core` next to `OrderStatus`) and use it at all three call sites.

## Ticket seeds for Plan 2 (from design spec)
- Postgres persistence + REST/WS API (Fastify) + dead-letter table + broker auth/ACL
- PixiJS dashboard (mine rmf-web patterns)
- Plan 3: 1C OData connector (mock + 1C:Fresh live)
